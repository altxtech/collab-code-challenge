<div id="playlists">
  <% @playlists.each do |playlist| %>
    <div class="playlist-item mb-3">
      <p>
        <%= link_to playlist.name, playlist_path(playlist) %>
      </p>
    </div>
  <% end %>
</div>
<%= link_to "New playlist", new_playlist_path, class: "btn btn-primary mt-3" %>